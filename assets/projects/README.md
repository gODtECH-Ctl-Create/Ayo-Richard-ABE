# Project assets

Create one folder per portfolio project using a lowercase slug.

Example:

`assets/projects/proqurement/`

Each project folder can contain up to three screenshots:

- `01.jpg`
- `02.jpg`
- `03.jpg`

The portfolio automatically crossfades between any available screenshots every 1.8 seconds. Projects without screenshots keep their default visual artwork.
